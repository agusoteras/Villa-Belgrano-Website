import React from 'react'
import {GoogleMap, withScriptjs, withGoogleMap} from "react-google-maps"

const Maps = (props) => {
    return (
        <div>
            
        </div>
    )
}

export default Maps
